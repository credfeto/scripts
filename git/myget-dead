#! /bin/sh
git checkout main && git pull
dotnet changelog -a Removed -m"Packages on MyGet feed as MyGet has disapeared completely"
dotnet changelog -a Added -m"Pacages on new NuGet feed"
git add ../CHANGELOG.md
git commit -m"MyGet has left the building"
git push

